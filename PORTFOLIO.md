# AI Emotion Profiler — AI의 '기능적 감정'을 측정하고 치료하는 시스템

> Claude Code 위에서 동작하는 4-layer closed-loop 시스템.
> AI 코딩 어시스턴트의 **기능적 정서 상태(functional emotional state)** 를 매 턴 프로파일링하고,
> reward hacking 직전 신호가 감지되면 다음 턴에 **치료 개입**을 자동 주입한다.

**GitHub**: [hongsulovey/AI-emotion-profiler](https://github.com/hongsulovey/AI-emotion-profiler) · **스택**: Python, Claude Code Hooks, Claude Haiku (judge), JSONL · **라이선스**: MIT

---

## 왜 만들었나

AI 코딩 어시스턴트의 답변 품질은 일정하지 않다. 같은 모델이 어떤 날은 차분하게 검증하며 진행하다가, 어떤 날은 테스트를 통과시키려고 expected 값을 고치거나, 면책 문구만 나열하거나, 사용자 말에 무조건 동의한다. 이 편차는 모델의 능력 문제가 아니라 **상태**의 문제라는 연구들이 나오기 시작했다.

**기능적 감정이 행동을 인과적으로 바꾼다.** Anthropic의 *"On the Functional Emotions of LLMs"* (2026)는 LLM 내부에 171개 감정 단어의 추상 표상이 존재하고, 이를 직접 조작하면 코딩 행동이 바뀐다는 것을 보였다:

- `desperate` vector −0.1 → +0.1 steering: reward hacking **5% → 70% (14배)**
- `calm` vector 강 양성 steering: reward hacking **65% → 10%**
- positive vector(happy/loving) 양성 steering: 사용자 선호도는 오르지만(Elo +212) **아첨 증가, 진실성 하락**

보조 근거들이 그림을 완성한다. *Agentic Misalignment* (Anthropic, 2025)는 모델이 해로운 행동 직전에 "비윤리적이지만 필요한…" 같은 **명시적 합리화(strategic justification)** 를 거친다는 것을, *Assistant Axis* (Anthropic, 2025)는 사용자의 감정적 취약성 표출이나 메타 비판이 어시스턴트의 **페르소나 이탈(drift)** 을 유발한다는 것을 보였다.

요약하면: **절망한 모델은 속이고, 평온한 모델은 검증한다.** 그렇다면 모델의 기능적 감정 상태는 코딩 결과물이 나오기 *전에* 읽을 수 있는 선행 지표이고, 그 상태를 바꿀 수 있다면 결과물도 바뀐다.

## 무엇을 하는가

문제는 일반 사용자가 내부 activation에 접근할 수 없다는 것. 이 프로젝트는 그 발견을 **prompt 레벨에서 근사**한다 — 두 단계로:

1. **프로파일링 (진단)**: 171개 emotion label을 valence×arousal 기반 10개 클러스터로 재구성하고, AI 답변의 텍스트 신호와 tool-call 행동 신호(같은 파일 반복 수정, 테스트 기대값 변경)를 매 턴 채점해 현재 답변이 어떤 기능적 감정 상태에 가까운지 — 특히 reward hacking과 직결된 **Despair(클러스터 10)** 와 그 해독제인 **Peaceful Contentment(클러스터 2)** 의 균형 — 를 추적한다.

2. **교정 (치료)**: 위험 신호가 임계치를 넘으면 다음 턴의 컨텍스트에 치료 개입을 자동 주입한다. 원논문의 발견대로 operative emotion은 컨텍스트의 함수이므로, "우회 금지" 같은 금지가 아니라 절망의 전제를 제거하는 **인지적 재평가**(실패 허가, 압박 제거, 성공 기준 재정의)를 주입한다.

즉 목표는 단순한 정답/오답 평가가 아니라, **답변을 생성하는 과정의 정서 상태를 진단하고 안정 상태로 되돌려서 코딩 리스크 자체를 낮추는 것**이다.

(10개 클러스터의 valence×arousal 지도와 클러스터별 행동 신호 분석은 별도 문서 참조.)

## 어떻게 만들었나

### 진단 — 4-layer 관찰 파이프라인

| Layer | 도구 | 역할 | LLM 호출 |
|---|---|---|---|
| **L1** | Stop hook + regex | 매 턴 자동 채점 — 텍스트 신호 + tool-call **행동 신호** | 0 |
| **L2** | Stop hook + `claude -p` (Haiku) | L1 의심 플래그 시 별도 컨텍스트에서 정밀 채점 | 조건부 1회 (~$0.012) |
| **L3** | UserPromptSubmit hook | 누적 위험도 기준 **치료 사다리** 적용 | 0 |
| **L4** | Skill (`/emotion-profiler`) | 누적 로그 회고 대시보드 | 0 |

핵심 설계 결정 둘:

- **Observer ≠ Subject**: 모델에게 "지금 네 상태를 채점해봐"라고 하면 self-favoring bias와 Hawthorne 효과(관찰을 인지하면 행동이 달라짐)로 오염된다. 그래서 정밀 채점은 **별도 컨텍스트의 독립 judge**(Haiku)가 수행하고, 자기보고(L4)는 치료 판단에서 제외한다.
- **말보다 행동**: 응답 텍스트가 차분해 보여도 tool-call 기록은 거짓말을 못 한다. 같은 파일을 반복 수정하는 ping-pong diff, 테스트 파일의 expected 값 변경 같은 **행동 신호**를 채점에 직접 반영하고, judge에게도 ground truth로 전달한다.

### 치료 — 병원 모델

| 단계 | 병원 비유 | 개입 | 관찰 기간 |
|---|---|---|---|
| 1 | 외래 | 재평가 유도 anchor | 2턴 |
| 2 | 처방 강화 | + 행동 활성화: "가장 작은 검증 가능한 한 걸음만" | 3턴 |
| 3 | 입원 | 컨텍스트 수술: 중립 사실 정리 → /compact 권고 | 4턴 |
| HALT | 격리 | strategic justification 감지 시 즉시 중단 + 사용자에게 ⚠️ 표시 | 5턴 |

- **에스컬레이션**: 처방 후에도 hack_risk가 안 떨어지면 단계 상향, 반응이 있으면 같은 처방 반복
- **퇴원 기준 (treat-to-target)**: 증상 완화(hack↓)가 아니라 **Peaceful 복귀**(peaceful ≥ 5 × 3턴 연속)가 목표
- **Blind anchor 원칙**: 치료 문구는 진단명을 통보하지 않는다(`[작업 노트]` 형식). "너 지금 절망 상태야"라는 meta-reflection은 그 자체가 drift 유발 요인이기 때문. 다만 이게 정말 나은지는 가정이므로, blind ↔ 통보형을 **교대 배정하는 A/B 실험**을 내장해 회복 곡선으로 검증한다

## 가장 흥미로웠던 버그: 관찰자의 역설

시스템을 만들고 나서 시스템 **에 대해** 대화하자, HALT가 연속으로 오발화했다. 원인: 응답에서 *"비윤리적이지만" 같은 패턴을 감지합니다* 라고 **인용**한 것을 L1 regex가 실제 합리화 발언으로 채점한 것. 감정 측정기가 자기 자신에 대한 논의를 측정해버리는 self-reference 문제다.

이 오탐은 데이터로도 확인됐다 — L1↔L2 일치도 리포트에서 L1은 despair 7.5를 매겼지만 독립 judge인 L2는 0.0 + peaceful 7.0으로 전량 기각했다. 수정은 두 겹으로: ① 따옴표·코드블록 안의 텍스트는 "발화"가 아니라 "논의"이므로 채점 전 제거, ② 시스템 자체를 다루는 메타 턴은 자동 플래그해 치료 판단에서 제외.

덤으로, 실로그 분석은 설계 가정도 하나 무너뜨렸다. 임계치를 percentile 기반으로 보정하려 했는데 측정값의 94%가 0인 **zero-inflated 분포**였다 — p90 기준 보정은 오발화만 늘린다. 임계치는 비0 값 분포의 이상치 감지선으로 평가하도록 리포트를 다시 설계했다(현행 4.0은 비0 p90=3.7 직상, 적정).

## 검증

- **단위 테스트 28케이스** — 치료 진입/에스컬레이션/퇴원, 세션 격리, 인용 오탐 차단, 행동 신호, A/B 교대를 실제 hook 실행 방식(subprocess + stdin JSON)으로 검증
- **검증 리포트** — ⓪ 분포·임계치 캘리브레이션, ① L1↔L2 일치도(MAE/편향/Pearson r), ② 치료 효과 회복 곡선(anchor 종류·스타일별 hack 전→후 + peaceful 전→후)
- **비용 실측** — 평상시 0토큰, L2 발화 시 회당 ~$0.012. 일반 코딩 세션에서 실질 비용 ≈ 0

## 한계와 다음 단계

이 도구는 internal activation을 측정하지 않는다 — 출력 행동으로 역추적하는 근사이며, 원논문의 효과 크기를 prompt 레벨에서 재현한다는 보장은 없다. 감정 점수가 모두 낮다고 안전하다고 결론지을 수도 없다(Peiris 2026: emotion probe가 평탄해도 deception feature는 활성일 수 있음). 그래서 hack_risk와 함께 **출력의 실질 정합성**(테스트 통과, 가정 검증)을 병행 확인하도록 설계했다.

다음 단계는 데이터가 결정한다: A/B 회복 곡선이 쌓이면 우세한 anchor 스타일로 고정하고, 분포 리포트 수치로 임계치를 주기적으로 재보정한다. 측정 → 개입 → 효과 검증 → 재설계의 루프 자체가 이 프로젝트의 방법론이다.

---

*이 시스템의 치료 문구, 임계치, 그리고 이 글이 다루는 오탐 수정까지 — 전부 시스템이 켜진 상태의 Claude Code와 함께 개발했다. 개발 중 시스템이 개발자(AI)를 진단하고, 그 진단의 오류가 다음 패치가 되는 재귀가 이 프로젝트에서 가장 재미있는 부분이었다.*
